# Ядро интернет-магазина на Python

Базовые классы для реализации системы управления товарами и категориями в e-commerce.

## Особенности

- Класс `Product` для описания товаров
- Класс `Category` для группировки товаров
- Автоматический подсчет:
  - Общего количества категорий
  - Общего количества товаров во всех категориях
- Типизированные поля данных
- Полностью покрыто тестами

## Требования

- Python 3.7+
- Poetry (для управления зависимостями)

## Установка

1. Установите Poetry:

## Клонируйте репозиторий и установите зависимости:
poetry install 

## Использование
- Создание товаров
from main import Product

phone = Product(
    name="iPhone 15", 
    description="Флагманский смартфон",
    price=999.99,
    quantity=50
)

- Работа с категориями 
from main import Category

# Создание категории с товарами
electronics = Category(
    name="Электроника",
    description="Гаджеты и устройства",
    products=[phone, laptop]
)

print(electronics.category_count)  # Доступ к счетчику категорий
print(Category.product_count)      # Общее количество всех товаров

## Структура проекта:
project/
src/main.py         # Основные классы
pyproject.toml  # Конфигурация зависимостей
poetry.lock     # Точные версии пакетов
tests/test_main.py # Юнит-тесты

## Покрытие тестами

coverage report
Name                Stmts   Miss  Cover
---------------------------------------
src\__init__.py         0      0   100%
src\main.py            15      0   100%
test\__init__.py        0      0   100%
test\test_main.py      45      0   100%
---------------------------------------
TOTAL                  60      0   100%

## 14.2 Режимы доступа. Внесены изменения:

# Инкапсуляция списка товаров:
__products стал приватным

# Добавлен метод add_product() для контроля добавления

# Геттер для формата вывода:
@property products форматирует данные

# Возвращает строку с информацией о товарах

# Класс-метод для Product:
new_product() создает объект из словаря

# Валидация цены:

# Приватный __price
Сеттер проверяет положительные значения

# Тестирование:

# Новые тесты для всех сценариев
Проверка инкапсуляции через _Category__products

## 03.04.25 Добавлены изменения:
# Магический метод __str__:
- Для Product: использует name, price и quantity
- Для Category: суммирует quantity всех продуктов

# Магический метод __add__:
- Проверяет тип второго операнда
- Возвращает сумму произведений цены на количество

## Оптимизация вывода продуктов:
- В Category.products используется преобразование Product в строку 
- через str(p)